# UI 工程化改造验收清单

更新时间：2026-07-10

## 本轮已落地内容

- 新增统一页面状态组件：加载中、空数据、加载失败、无权限/无结果可复用。
- 表格组件支持统一加载、错误、空状态和重试入口。
- 删除类操作改为统一二次确认弹窗，不再直接使用浏览器默认确认框。
- 多个页面补齐请求失败提示，避免接口失败时被误认为“暂无数据”。
- 会话详情、客户记忆、对话复盘、模拟训练、设置、客户、知识库、总览等页面补齐局部错误状态。
- 新增浏览器级滚动冒烟测试，覆盖平台管理员和商户管理员的主要页面。
- 滚动冒烟测试已扩展为交互冒烟测试：验证总览、模型调用、客户、会话的时间筛选请求确实携带 `startAt`、`endAt` 和 `timeZone`。
- 模型调用页已增加请求级验证：供应商、调用类型和成功/失败状态筛选会真实传到统计接口，并联动顶部统计、成功率和明细表。
- 设置页开启“话本流程”前会校验当前商户是否已有启用且有效的话本流程，避免开关显示成功但客户实际走系统兜底或普通回复。
- 验证客户页“当前筛选导出”和会话页“导出全部 / 导出当前账号”会发起正确导出请求。
- 客户页已改为服务端分页：列表按页读取，客户总数继续使用数据库 `COUNT(*)` 统计，不再因为前端每页条数影响总数。
- 修复会话页在北京时间模式下未显式传递 `Asia/Shanghai` 的问题，便于统计筛选和问题排查保持一致。
- 新增后台文案冒烟检查，拦截常见英文按钮词和高频英文夹中文功能名，包括 `AI供应商`、`AI建议`、`A2C AI` 等可见残留。
- 修复客户索引重建时可能取到旧会话资料的问题。
- 模型调用页已从主入口拆分到独立页面模块，后续排查供应商、调用类型、失败原因时不再继续挤压 `main.tsx`。
- 后台账号页已从主入口拆分到独立页面模块，便于后续继续统一账号权限、删除确认和重置密码体验。
- 样本页已从主入口拆分到独立页面模块，训练样本上传、分页、编辑和删除逻辑后续可独立优化。
- 意图学习页已从主入口拆分到独立页面模块，方便后续继续优化搜索、审核流和意图样例沉淀。
- 训练中心/素材页已从主入口拆分到独立页面模块，上传学习、粘贴学习、详情查看和删除操作后续可集中维护。
- 会话详情右侧训练辅助面板已拆分到会话模块，脚本进度、快捷回复、知识匹配、复盘沉淀和客户资料页签后续可独立优化。
- 话本流程页已从主入口拆分到独立页面模块，流程导入、内置 11 步创建、节点编辑、启用校验和版本恢复后续可集中维护。
- 会话页本体已从主入口拆分到独立页面模块，平台/商户会话列表、客服账号层级、客户列表、主动新建对话和会话详情后续可集中优化。
- 配置/设置页已从主入口拆分到独立页面模块，国家配置、A2C 同步、邀请码池、老师 TG 链接、教程图和高风险开关后续可集中维护。
- 通用表格和筛选栏已补齐统一元信息、横向滚动提示、紧凑密度和显式筛选字段标签，减少各页面重复处理表格滚动、搜索条件说明和空/错状态展示。
- 前端样式已开始分层：设计 token、全局 reset、按钮和输入框基础样式已拆到 `frontend/src/styles/tokens.css`，后续可继续按 layout、components、pages 迁移。
- Shell 布局、侧边栏、顶部栏、在线状态和时间切换器基础样式已拆到 `frontend/src/styles/layout.css`，业务页面样式继续留在主样式文件中渐进迁移。
- 提示条、统一状态、确认弹窗和 Toast 样式已拆到 `frontend/src/styles/feedback.css`，与通用页面状态组件和高风险操作确认保持同一维护入口。
- 卡片区块、指标卡、工具栏、筛选栏、表格、状态标签、分页和徽标基础样式已拆到 `frontend/src/styles/components.css`，页面局部覆盖继续保留在主样式文件中。
- 启动页、登录页和品牌锁定区样式已拆到 `frontend/src/styles/entry.css`，后台业务页主样式继续变薄。
- 总览和模型调用页面的基础布局样式已拆到 `frontend/src/styles/analytics.css`，后续可继续把页面专属样式按模块迁移。
- 客户页列表、客户会话历史和客户详情中的会话卡片样式已拆到 `frontend/src/styles/customers.css`，便于后续单独治理客户页信息密度。
- 设置页中的国家配置、配置检测、注册教程图片、智能回复开关、A2C 账号和邀请码池样式已拆到 `frontend/src/styles/settings.css`，减少配置页交互问题排查成本。
- 训练中心、素材上传、模拟消息列表和上传引导区样式已拆到 `frontend/src/styles/training.css`，让训练相关页面的视觉规则集中维护。
- 后台分栏、工作面板、详情标题、表单区、商户创建表单、粘性操作条和记忆卡片等跨页面工作区样式已拆到 `frontend/src/styles/workspace.css`。
- 会话工作台中的客服账号列表、客户列表、会话卡片、导出工具、消息气泡、媒体预览和发送栏基础样式已拆到 `frontend/src/styles/conversations.css`，保留主样式中的后续产品设计覆盖顺序。
- 话本流程编辑器与意图学习页面的基础布局、节点卡片、版本行、学习指标和详情卡片样式已拆到 `frontend/src/styles/flow-learning.css`。
- 产品设计刷新层、后台密度、卡片/表格/模拟训练/复盘基础视觉覆盖已拆到 `frontend/src/styles/product-refresh.css`，加载在基础模块之后。
- 2026-06 页面适配、会话工作台密度调整、复盘折叠、客户列表和窄屏适配覆盖规则已拆到 `frontend/src/styles/ui-fit-pass.css`，加载在主样式之后。
- 全局响应式兜底、防横向溢出、窄屏会话布局、表格和表单自适应规则已拆到 `frontend/src/styles/responsive-guardrails.css`，并在最终覆盖文件之前引入。
- 页面滚动安全、分析页主滚动、话本节点选中态可读性和最后覆盖规则已拆到 `frontend/src/styles/final-overrides.css`，并在 `main.tsx` 中放在主样式之后引入以保持覆盖顺序。
- 平台商户管理页已从入口文件抽到 `frontend/src/merchants/MerchantsPage.tsx`，入口文件只保留登录、导航、角色视图和页面路由。
- 登录页、后台壳层和页面注册已从入口文件拆到 `frontend/src/app/`；导航、中文页面名和角色可访问页面由同一个注册模块维护，未知地址或越权页面会安全回到总览。
- 通用列表加载模块已补齐加载、错误、重试和旧请求防覆盖状态；国家选项、商户选项、客服账号等辅助数据加载失败时不再静默显示为空。
- 意图学习列表已补齐加载中、加载失败、重新加载和明确空状态，接口异常不会再伪装成“暂无候选”。
- 后端已删除 700 多行不再参与主链路的旧 Gemini 回复实现，只保留统一 AI 供应商层使用的 Gemini 传输适配器；自然化文本保护规则已合并到唯一实现。
- 设置页已重组为“运行模式、A2C 接入、智能供应商、国家与引导、客服账号与邀请码、TG 接管”六个配置分组；桌面使用粘性左侧导航，窄屏自动切换为横向导航。
- 设置分组会显示当前配置状态和修改影响；A2C、智能供应商、TG 密钥不再混在同一张表单，供应商切换时只展示当前供应商对应的密钥和模型。
- 设置页浏览器验收已覆盖六个分组导航、MiniMax/DeepSeek/Gemini兼容字段切换、三个高风险运行开关和 1024 宽度滚动。
- 新增统一操作日志：后台新增、修改、删除、导入、同步、恢复、启用、发送和标记已读等写操作会记录操作人、商户、资源、目标、结果和时间。
- 操作日志不保存请求正文、密码或密钥；被后端拒绝的 403 写操作也会留痕，便于排查越权尝试。
- 平台管理员可查看全局操作日志，商户管理员只能查看当前商户，商户运营不显示操作日志入口。
- 会话辅助区已删除“回复合适 / 回复不合适 / 不准确 / 不完整”等只弹成功提示但没有真实写入数据的假操作，仅保留填入回复、生成复盘和审核沉淀等有实际结果的入口。
- 智能体启停已改为明确的二元开关；修改后页面会提示“保存后生效”，保存启用或停用必须二次确认并说明影响范围。

## 新增验证命令

```bash
npm run ui:scroll-smoke
```

该命令会：

1. 先执行生产构建。
2. 使用临时 SQLite 数据库启动本地服务。
3. 自动登录平台管理员。
4. 创建一个临时商户管理员。
5. 分别用平台管理员和商户管理员访问主要后台页面。
6. 对页面内可滚动区域执行滚动到底部检查。
7. 验证关键时间筛选、模型调用供应商/调用类型/状态筛选、客户/会话导出、会话客户栏与辅助区收展、智能体配置保存、话本流程创建编辑删除、设置页高风险开关点击反馈。
8. 话本流程启用前使用与后端一致的完整性规则检查流程编号、名称、标准话术、跳转、注册链接、邀请码和老师TG链接变量；未通过检查时不能启用。
9. 使用独立商户运营账号验证权限：仅显示总览、智能体、话本、客户、会话、接管和设置；智能体、话本、客户删除和设置核心配置保持只读。
10. 后端同步收紧商户运营权限：训练样本/素材导入和样本修改只允许商户管理员，避免绕过前端直接写入训练数据。
11. 设置页保存后自动生成配置版本，记录修改人、时间和变更字段；版本列表不返回密钥快照，管理员恢复旧版本时必须二次确认并生成新的恢复记录。
12. 智能体配置保存后自动生成版本，启停需确认影响后再保存，管理员可二次确认恢复，商户运营只读可查看；恢复后的角色、语气、目标和边界立即回填并保留恢复记录。
13. 操作日志页的操作角色、动作、资源和状态均使用中文显示；商户数据隔离和商户运营无入口状态纳入验收。
14. 输出截图到 `output/playwright/scroll-smoke/`。

该命令不会连接线上数据库，也不会修改真实客户数据。

## 已纳入滚动验收的页面

平台管理员：

- 总览
- 模型调用
- 操作日志
- 商户
- 后台账号
- 配置
- 智能体配置
- 客户
- 话本流程
- 意图学习
- 素材
- 知识库
- 样本
- 会话
- 接管

商户管理员：

- 总览
- 模型调用
- 操作日志
- 训练中心
- 模拟训练
- 智能体配置
- 话本流程
- 意图学习
- 客户
- 会话
- 接管
- 设置

## 发布前建议必跑

```bash
npm run typecheck
npm test
npm run ui:copy-smoke
npm run build
npm run ui:scroll-smoke
```

## 本轮还未完全解决的结构问题

这些问题已经在 `docs/ui-optimization-master-analysis.md` 中列为后续工程化方向。本轮已经完成主要页面模块拆分、样式分层、页面状态、滚动与关键按钮验收，剩余风险集中在更深层的组件抽象和配置治理：

- `frontend/src/main.tsx` 已降为路由/壳层入口，但后续仍可继续把权限、导航和页面注册表抽成独立配置。
- `frontend/src/styles.css` 已拆出 token、layout、components、entry、analytics、customers、settings、training、workspace、conversations、flow-learning 等文件；剩余内容主要是历史产品设计覆盖层、响应式规则和滚动安全规则，后续应按 `refresh.css`、`responsive.css`、`scroll-safety.css` 继续拆，但必须保持 CSS 覆盖顺序。
- 表格组件已补齐统一状态和横向滚动提示，但仍可升级为带列配置、详情抽屉和批量操作的数据表格。
- 设置页、会话页、话本流程页已拆出页面模块和主要样式块，后续重点是进一步抽取“配置组”“会话三栏”“节点编辑抽屉”等可复用组件。
- 配置影响范围提示已覆盖设置分组，设置、智能体和话本流程已有版本恢复；统一操作日志已覆盖国家、账号、邀请码、导师 TG 链接和主要配置写操作，后续可继续补充字段级差异展示。
- 商户运营的前端只读边界已覆盖智能体、话本、客户和设置，后续仍需把统一权限提示扩展到会话复盘沉淀等细粒度操作。
- 部署前仍需要跑完整发布门禁并确认 Render `live`，本轮暂不部署。

## 后续改造优先级

1. 话本流程页：继续优化节点拖拽排序和未保存离开提醒；节点导航、分组编辑、启用前完整性校验、问题定位和高风险确认已完成。
2. 会话页：继续统一消息原文/译文密度和多条操作反馈；三栏布局、客户工具区独立滚动、客户栏与右侧辅助区收展已完成。
3. 设置页：继续拆分独立保存和独立检测；六类配置分组、供应商联动和高风险开关验收已完成。
4. 客户页：继续优化详情抽屉、导出入口和搜索条件的操作密度。
5. 模型调用页：供应商/类型筛选、失败原因排行、调用明细抽屉。
